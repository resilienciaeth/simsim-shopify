<link rel="stylesheet" href="{{ 'component-search.css' | asset_url }}" media="print" onload="this.media='all'">
<link
  rel="stylesheet"
  href="{{ 'component-cart-notification.css' | asset_url }}"
  media="print"
  onload="this.media='all'"
>

<style>
  .header-wrapper {
    width: 100%;
    border-bottom: none; /* remove bottom bar */
    background: transparent; /* remove background bar */
    position: fixed;
    top: 0;
    left: 0;
    z-index: 9998;
  }

  /* Add padding to body content to account for fixed header */
  body {
    padding-top: 180px; /* Desktop margin */
  }

  /* Mobile margin */
  @media screen and (max-width: 1199px) {
    body {
      padding-top: 120px;
    }
  }

  /* Remove padding for brand page */
  .template-page.page-brand-page body,
  body.template-page.page-brand-page,
  .template-page.brand-page body,
  body.template-page.brand-page {
    padding-top: 0 !important;
  }

    /* Ensure consistent font family and size across entire website */
  html, body, * {
    font-family: var(--font-body-family) !important;
    font-size: 12px !important;
  }

  h1, h2, h3, h4, h5, h6, .h1, .h2, .h3, .h4, .h5, .h6 {
    font-family: var(--font-heading-family) !important;
    font-size: 12px !important;
  }

  /* Ensure all specific text elements are 12px */
  p, span, div, a, button, input, textarea, select, label, li, td, th {
    font-size: 12px !important;
  }

  /* Ensure price elements are 12px */
  .price, .price *, .card-information, .card-information * {
    font-size: 12px !important;
  }

  /* Ensure navigation and menu elements are 12px */
  nav, nav *, .header, .header *, .footer, .footer * {
    font-size: 12px !important;
  }

  /* Ensure form elements are 12px */
  .field, .field *, .form, .form *, .button, .button * {
    font-size: 12px !important;
  }

  /* Override desktop-specific media queries for consistent 12px */
  @media screen and (min-width: 750px) {
    .price, .price *, .price--large, .price--large * {
      font-size: 12px !important;
    }

    .card__heading, .card__heading *, .h5, .h5 * {
      font-size: 12px !important;
    }

    .card--horizontal .card__heading,
    .card--horizontal .price__container .price-item,
    .card--horizontal__quick-add {
      font-size: 12px !important;
    }
  }

  @media screen and (min-width: 990px) {
    .price, .price *, .price--large, .price--large * {
      font-size: 12px !important;
    }

    .card__heading, .card__heading *, .h5, .h5 * {
      font-size: 12px !important;
    }
  }

  @media screen and (min-width: 1200px) {
    .price, .price *, .price--large, .price--large * {
      font-size: 12px !important;
    }

    .card__heading, .card__heading *, .h5, .h5 * {
      font-size: 12px !important;
    }
  }

  /* Remove padding for product pages and brand pages */
  html body.template-product,
  body.template-product,
  .template-product body,
  html body.template-page.page-brand-page,
  body.template-page.page-brand-page,
  .template-page.page-brand-page body {
    padding-top: 0 !important;
    margin-top: 0 !important;
  }

  @media screen and (max-width: 1199px) {
    html body.template-product,
    body.template-product,
    .template-product body,
    html.template-product body,
    body.template-product *,
    html body.template-page.page-brand-page,
    body.template-page.page-brand-page,
    .template-page.page-brand-page body {
      padding-top: 0 !important;
      margin-top: 0 !important;
    }
  }

  /* Mobile and Desktop content styling for miscellaneous page */
  /* @media screen and (min-width: 1200px) { */ /* Apply to all screen sizes */
    /* Remove all default padding and constraints */
    .template-page *,
    .template-page .page-width.page-width--narrow[class*='section-template'][class*='padding'],
    .template-page .page-width.page-width--narrow,
    .template-page div[class*='section-template'][class*='main-padding'] {
      padding: 0 !important;
      margin: 0 !important;
      max-width: none !important;
    }

    /* Force container to full width with 30px padding - override Shopify's 1200px max-width */
    .template-page .page-width,
    .template-page .page-width--narrow {
      width: 100vw !important;
      max-width: none !important; /* Override the 1200px constraint */
      margin: 0 !important;
      padding: 30px !important;
      position: relative;
      left: 0 !important;
      box-sizing: border-box !important;
    }

    /* Clean 25% width layout - no debugging styles */

    /* Responsive width rules for text content */
    html body .template-page h1.main-page-title.page-title.h0,
    html body .template-page div.rte,
    html body .template-page h1,
    html body .template-page .main-page-title,
    html body .template-page .page-title,
    html body .template-page .rte,
    body[class*='page'] h1,
    body[class*='page'] .main-page-title,
    body[class*='page'] .page-title,
    body[class*='page'] .rte,
    h1,
    .main-page-title,
    .page-title,
    .rte {
      padding: 0 !important;
      margin: 0 !important;
      text-align: left !important;
      box-sizing: border-box !important;
      display: block !important;
      clear: both !important;
    }

    /* Desktop: 25% width */
    @media screen and (min-width: 1200px) {
      html body .template-page h1.main-page-title.page-title.h0,
      html body .template-page div.rte,
      html body .template-page h1,
      html body .template-page .main-page-title,
      html body .template-page .page-title,
      html body .template-page .rte,
      body[class*='page'] h1,
      body[class*='page'] .main-page-title,
      body[class*='page'] .page-title,
      body[class*='page'] .rte,
      h1,
      .main-page-title,
      .page-title,
      .rte {
        width: 25vw !important;
        max-width: 25vw !important;
        min-width: 25vw !important;
        float: left !important;
        flex-basis: 25% !important;
        flex-grow: 0 !important;
        flex-shrink: 0 !important;
      }
    }

    /* Mobile: full width */
    @media screen and (max-width: 1199px) {
      html body .template-page h1.main-page-title.page-title.h0,
      html body .template-page div.rte,
      html body .template-page h1,
      html body .template-page .main-page-title,
      html body .template-page .page-title,
      html body .template-page .rte,
      body[class*='page'] h1,
      body[class*='page'] .main-page-title,
      body[class*='page'] .page-title,
      body[class*='page'] .rte,
      h1,
      .main-page-title,
      .page-title,
      .rte {
        width: 100% !important;
        max-width: 100% !important;
        float: none !important;
      }
    }

    /* Title styling - 12px uppercase black */
    html body .template-page .page-title,
    html body .template-page .main-page-title,
    html body .template-page h1.main-page-title.page-title.h0,
    html body .template-page h1,
    body[class*='page'] .page-title,
    body[class*='page'] .main-page-title,
    body[class*='page'] h1,
    .template-page h1,
    .template-page .h0,
      .h0 {
    text-transform: uppercase !important;
      color: #000000 !important;
      margin-bottom: 1rem !important;
      margin-top: 0 !important;
      font-weight: normal !important;
      letter-spacing: 0.5px !important;
    }

    /* Content text - 12px, normal case, 125% line height, black, 18px indent */
    html body .template-page .rte,
    html body .template-page div.rte,
    html body .template-page .rte *,
    html body .template-page .rte p,
    html body .template-page .rte div,
    html body .template-page .rte span,
    html body .template-page .rte strong,
    html body .template-page .rte em,
    html body .template-page p,
    body[class*='page'] .rte,
    body[class*='page'] .rte *,
    body[class*='page'] p,
    .template-page .rte,
    .template-page .rte *,
    .template-page p,
    .rte,
    .rte *,
    .rte p,
    .rte div,
    .rte span,
    /* Target everything that could be text */
    .template-page div,
    .template-page span,
    .template-page strong,
    .template-page em,
    .template-page text {
      font-size: 12px !important;
      text-transform: none !important;
      color: #000000 !important;
      line-height: 125% !important;
      text-indent: 18px !important;
      margin-bottom: 1rem !important;
      margin-top: 0 !important;
      font-weight: normal !important;
      letter-spacing: 0.5px !important;
    }

    /* Override CSS custom properties */
    .template-page {
      --section-padding-horizontal: 0px !important;
      --page-width-margin: 0px !important;
      --spacing-sections-desktop: 0px !important;
      --spacing-sections-mobile: 0px !important;
    }

    /* Maximum specificity override */
    html body .template-page #MainContent .shopify-section .page-width.page-width--narrow {
      padding: 0 !important;
      margin: 0 !important;
      width: 100vw !important;
      max-width: 100vw !important;
    }
  }

  .custom-header {
    width: 100%;
  }

  .custom-navbar {
    display: grid; /* true three-column layout */
    grid-template-columns: 1fr auto 1fr; /* left | logo | right */
    align-items: center;
    padding: 2rem 1.6rem; /* vertical 2rem, horizontal mobile 16px */
    font-size: 12px;
    text-transform: uppercase;
    letter-spacing: 1px;
    font-weight: 500;
    width: 100%;
    min-height: 60px;
    color: inherit;
  }

  .navbar-left,
  .navbar-right {
    display: flex;
    gap: 1.6rem; /* 16px base gap */
    min-width: 0; /* Prevent overflow */
  }

  .navbar-left {
    justify-self: start;
    justify-content: flex-start;
  }
  .navbar-right {
    justify-self: end;
    justify-content: flex-end;
  }

  .navbar-center {
    display: flex;
    align-items: center;
    justify-self: center; /* sits in the middle column */
  }

  .navbar-link {
    text-decoration: none;
    color: inherit;
    font-weight: 500;
    transition: opacity 0.2s ease;
  }

  @media screen and (min-width: 1200px) {
    .navbar-link:hover {
      opacity: 0.5;
    }
  }

  .navbar-logo {
    font-size: 18px;
    font-weight: bold;
    text-decoration: none;
    color: inherit;
    text-transform: uppercase;
    letter-spacing: 2px;
  }

  .navbar-logo-image {
    max-height: 40px;
    width: auto;
  }

  /* no icons - text only */


  /* Mobile responsive */
  @media screen and (max-width: 1199px) {
    .navbar-left,
    .navbar-right {
      gap: 1.5rem;
    }

    .custom-navbar {
      font-size: 12px; /* keep 12px on mobile too */
      padding: 1.5rem 1.6rem; /* keep 16px sides on mobile */
    }

    /* Ensure link text is 12px on mobile */
    .navbar-link {
      font-size: 12px !important;
    }

    /* Force all navbar text to be 12px on mobile */
    .custom-navbar,
    .custom-navbar *,
    .navbar-link,
    .navbar-menu {
      font-size: 12px !important;
    }

    .navbar-logo {
      font-size: 16px;
    }

    .navbar-logo-image {
      max-height: 32px;
    }
  }

  @media screen and (min-width: 1200px) {
    .custom-navbar {
      padding-left: 3rem; /* 30px desktop sides */
      padding-right: 3rem;
    }

    .navbar-left,
    .navbar-right {
      gap: 3rem; /* 30px desktop spacing between items */
    }
  }

  /* Variant styles */
  .nav-variant--dark .custom-navbar,
  .nav-variant--dark .navbar-link {
    color: #000;
  }
  .nav-variant--light .custom-navbar,
  .nav-variant--light .navbar-link {
    color: #fff;
  }

  /* Mobile menu visibility rules */
  .navbar-menu {
    display: none;
  }

  @media screen and (max-width: 1199px) {
    .navbar-left .navbar-link:not(.navbar-menu) {
      display: none;
    }
    .navbar-menu {
      display: inline-flex;
    }
    .nav-hide-mobile {
      display: none !important;
    }
  }

  /* Unified Mobile Menu System */
  .unified-menu-drawer {
    position: fixed;
    top: 0;
    left: 0;
    width: 70vw;
    height: 100vh;
    background: #4e8926;
    color: #000; /* black text per design */
    padding: 0 16px 16px 16px; /* 16px sides; top spacing handled by close button */
    transform: translateX(-100%);
    transition: transform 300ms ease;
    z-index: 9999;
    display: flex;
    flex-direction: column;
    gap: 0; /* we'll manage spacing explicitly */
    will-change: transform; /* Optimize for smooth animations */
  }

  .unified-menu-drawer .menu-close {
    position: absolute;
    top: 20px; /* 20px from top */
    left: 16px; /* 16px from left */
    background: transparent;
    border: none;
    padding: 0;
    margin: 0;
    color: #000;
    font-size: 12px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    cursor: pointer;
  }

  /* Hide mobile drawer on desktop */
  @media screen and (min-width: 1200px) {
    .unified-menu-drawer {
      display: none !important;
    }
  }

  @media screen and (max-width: 1199px) {
    .unified-menu-drawer nav {
      display: flex;
      flex-direction: column;
      gap: 10px !important; /* exact 10px between items */
      margin-top: 90px; /* 90px space after CLOSE */
    }

    .unified-menu-drawer .navbar-link {
      color: #000;
      text-decoration: none;
      font-size: 24px !important; /* force 24px menu options size */
      text-transform: uppercase; /* make options uppercase */
      padding: 0; /* no extra padding */
      border: none; /* remove any borders */
      margin: 0; /* ensure no extra spacing */
      line-height: 1; /* tighten line box so gap controls spacing */
      display: block; /* consistent height calculation */
      font-weight: normal; /* ensure normal weight */
    }

    /* Add 30px gap between the two groups (after 3rd link) */
    .unified-menu-drawer nav a:nth-child(4) {
      margin-top: 20px; /* 10px base gap + 20px = 30px total */
    }

    .unified-menu-drawer .navbar-link:hover {
      opacity: 0.8;
    }

    /* Footer menu options */
    .menu-footer {
      margin-top: auto; /* Push to bottom */
      padding-top: 40px;
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .menu-footer-link {
      color: #000;
      text-decoration: none;
      font-size: 12px !important;
      text-transform: uppercase;
      padding: 0;
      border: none;
      margin: 0;
      line-height: 1;
      display: block;
      font-weight: normal;
    }

    .menu-footer-link:hover {
      opacity: 0.8;
    }

    /* Social media links */
    .menu-social {
      padding-top: 30px;
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .menu-social-link {
      color: #000;
      text-decoration: none;
      font-size: 12px !important;
      text-transform: uppercase;
      padding: 0;
      border: none;
      margin: 0;
      line-height: 1;
      display: block;
      font-weight: normal;
    }

    .menu-social-link:hover {
      opacity: 0.8;
    }

    /* Menu open state */
    body.unified-menu-open {
      overflow: hidden;
    }

    body.unified-menu-open .unified-menu-drawer {
      transform: translateX(0);
    }

    /* Push content and header content (but not the drawer) when menu is open */
    body.unified-menu-open #MainContent,
    body.unified-menu-open main,
    body.unified-menu-open .content-for-layout,
    body.unified-menu-open .custom-header {
      transform: translateX(70vw) !important;
      transition: transform 300ms ease !important;
    }
  }

  /* Hide text on very small screens, show icons only */
  @media screen and (max-width: 768px) {
    .navbar-text {
      display: none;
    }

    .navbar-left,
    .navbar-right {
      gap: 1rem;
    }

    .custom-navbar {
      padding: 1rem 1.6rem; /* ensure 16px side padding on small mobile */
    }

    .navbar-logo {
      font-size: 14px;
    }

    .navbar-logo-image {
      max-height: 28px;
    }
  }
</style>

<header class="header-wrapper color-{{ section.settings.color_scheme }} gradient nav-variant--{{ section.settings.nav_variant }}">
  <div class="custom-header">
    <nav class="custom-navbar">
      <!-- Left Navigation -->
      <div class="navbar-left">
        <a href="#" class="navbar-link navbar-menu" data-unified-menu-toggle>MENU</a>
        <a href="{{ routes.collections_url }}" class="navbar-link nav-hide-mobile">
          <span class="navbar-text">SHOP</span>
        </a>
        <a href="{{ routes.pages_url }}/explore" class="navbar-link nav-hide-mobile">
          <span class="navbar-text">EXPLORE</span>
        </a>
        <a href="/pages/brand" class="navbar-link nav-hide-mobile">
          <span class="navbar-text">BRAND</span>
        </a>
      </div>

      <!-- Center Logo -->
      <div class="navbar-center">
        <a href="{{ routes.root_url }}" class="navbar-logo">
          {%- assign logo_alt = shop.name | escape -%}
          {%- assign dark_logo_url = 'https://cdn.shopify.com/s/files/1/0911/2394/1654/files/logo_2.png?v=1756148711' -%}
          {%- assign light_logo_url = 'https://cdn.shopify.com/s/files/1/0911/2394/1654/files/Group_343.png?v=1756148728' -%}
          <img
            src="{%- if section.settings.nav_variant == 'light' -%}{{ light_logo_url }}{%- else -%}{{ dark_logo_url }}{%- endif -%}"
            alt="{{ logo_alt }}"
            class="navbar-logo-image"
            width="120"
            height="32"
            style="height: 32px; width: auto;"
          >
        </a>
      </div>

      <!-- Right Navigation -->
      <div class="navbar-right">
        <a href="{{ routes.search_url }}" class="navbar-link nav-hide-mobile">SEARCH</a>

        {%- if shop.customer_accounts_enabled -%}
          <a
            href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}"
            class="navbar-link nav-hide-mobile"
            >ACCOUNT</a
          >
        {%- endif -%}

        <a href="{{ routes.cart_url }}" class="navbar-link" id="cart-icon-bubble">
          {%- if cart != empty -%}
            CART({{ cart.item_count }})
          {%- else -%}
            CART
          {%- endif -%}
        </a>
      </div>
    </nav>
  </div>
</header>

<!-- Unified Menu Drawer (outside header) -->
<div class="unified-menu-drawer" id="UnifiedMenuDrawer" aria-hidden="true">
  <button type="button" class="menu-close" data-unified-menu-close>CLOSE</button>
  <nav>
    <a href="{{ routes.collections_url }}" class="navbar-link">Shop</a>
    <a href="{{ routes.pages_url }}/explore" class="navbar-link">Explore</a>
    <a href="/pages/brand" class="navbar-link">Brand</a>
    <a href="{{ routes.search_url }}" class="navbar-link">Search</a>
    {%- if shop.customer_accounts_enabled -%}
      <a
        href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}"
        class="navbar-link"
        >Account</a
      >
    {%- endif -%}
  </nav>

  <!-- Footer Menu Options -->
  <div class="menu-footer">
    <a href="{{ routes.pages_url }}/privacy-policy" class="menu-footer-link">PRIVACY POLICY</a>
    <a href="{{ routes.pages_url }}/shipping-and-return" class="menu-footer-link">SHIPPING AND RETURN</a>
    <a href="{{ routes.pages_url }}/terms-and-conditions" class="menu-footer-link">TERMS AND CONDITIONS</a>
  </div>

  <!-- Social Media Links -->
  <div class="menu-social">
    <a href="#" class="menu-social-link">FACEBOOK</a>
    <a href="#" class="menu-social-link">INSTAGRAM</a>
    <a href="#" class="menu-social-link">TIKTOK</a>
  </div>
</div>

{% comment %}
  {%- render 'cart-notification', color_scheme: section.settings.color_scheme -%}
{% endcomment %}

<script type="application/ld+json">
  {
    "@context": "http://schema.org",
    "@type": "Organization",
    "name": {{ shop.name | json }},
    {% if settings.logo %}
      "logo": {{ settings.logo | image_url: width: 500 | prepend: "https:" | json }},
    {% endif %}
    "sameAs": [
      {{ settings.social_twitter_link | json }},
      {{ settings.social_facebook_link | json }},
      {{ settings.social_pinterest_link | json }},
      {{ settings.social_instagram_link | json }},
      {{ settings.social_tumblr_link | json }},
      {{ settings.social_snapchat_link | json }},
      {{ settings.social_youtube_link | json }},
      {{ settings.social_vimeo_link | json }}
    ],
    "url": {{ request.origin | append: page.url | json }}
  }
</script>

<script>
  // Unified Menu System
  (function () {
    var toggle = document.querySelector('[data-unified-menu-toggle]');
    var drawer = document.getElementById('UnifiedMenuDrawer');

    if (!toggle || !drawer) return;

    // Toggle menu
    toggle.addEventListener('click', function (e) {
      e.preventDefault();
      var isOpen = document.body.classList.toggle('unified-menu-open');
      drawer.setAttribute('aria-hidden', String(!isOpen));
    });

    // Close button inside drawer
    var closeBtn = drawer.querySelector('[data-unified-menu-close]');
    if (closeBtn) {
      closeBtn.addEventListener('click', function () {
        closeMenu();
      });
    }

    // Close on ESC key
    document.addEventListener('keydown', function (e) {
      if (e.key !== 'Escape') return;
      closeMenu();
    });

    // Close when clicking pushed content
    function bindCloseOnClick(selector) {
      var el = document.querySelector(selector);
      if (!el) return;
      el.addEventListener('click', function (e) {
        if (!document.body.classList.contains('unified-menu-open')) return;
        // Don't close if clicking on the header or menu
        if (e.target.closest('.header-wrapper') || e.target.closest('.unified-menu-drawer')) return;
        closeMenu();
      });
    }

    // Bind to main content areas
    bindCloseOnClick('#MainContent');
    bindCloseOnClick('main');
    bindCloseOnClick('.content-for-layout');

    // Close when any menu link is clicked
    var drawerNav = drawer.querySelector('nav');
    if (drawerNav) {
      drawerNav.addEventListener('click', function (e) {
        var target = e.target.closest('a');
        if (!target) return;
        closeMenu();
      });
    }

    function closeMenu() {
      document.body.classList.remove('unified-menu-open');
      drawer.setAttribute('aria-hidden', 'true');
    }

    // If the unified menu is open and user clicks CART, close menu then open cart
    var cartLinkWhenMenuOpen = document.getElementById('cart-icon-bubble');
    if (cartLinkWhenMenuOpen) {
      cartLinkWhenMenuOpen.addEventListener('click', function (e) {
        if (!document.body.classList.contains('unified-menu-open')) return;
        e.preventDefault();
        closeMenu();
        // Re-trigger the click after menu closes so the cart drawer can open
        setTimeout(function () {
          cartLinkWhenMenuOpen.click();
        }, 20);
      });
    }
  })();
</script>

{%- if request.page_type == 'index' -%}
  {% assign potential_action_target = request.origin | append: routes.search_url | append: '?q={search_term_string}' %}
  <script type="application/ld+json">
    {
      "@context": "http://schema.org",
      "@type": "WebSite",
      "name": {{ shop.name | json }},
      "potentialAction": {
        "@type": "SearchAction",
        "target": {{ potential_action_target | json }},
        "query-input": "required name=search_term_string"
      },
      "url": {{ request.origin | append: page.url | json }}
    }
  </script>
{%- endif -%}

{% schema %}
{
  "name": "t:sections.header.name",
  "class": "section-header",
  "settings": [
    {
      "type": "select",
      "id": "nav_variant",
      "label": "Navbar variant",
      "options": [
        { "value": "dark", "label": "Dark (black text)" },
        { "value": "light", "label": "Light (white text)" }
      ],
      "default": "dark"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "checkbox",
      "id": "show_line_separator",
      "default": true,
      "label": "t:sections.header.settings.show_line_separator.label"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.header__1.content"
    },
    {
      "type": "select",
      "id": "logo_position",
      "options": [
        {
          "value": "middle-left",
          "label": "t:sections.header.settings.logo_position.options__1.label"
        },
        {
          "value": "top-left",
          "label": "t:sections.header.settings.logo_position.options__2.label"
        },
        {
          "value": "top-center",
          "label": "t:sections.header.settings.logo_position.options__3.label"
        },
        {
          "value": "middle-center",
          "label": "t:sections.header.settings.logo_position.options__4.label"
        }
      ],
      "default": "middle-left",
      "label": "t:sections.header.settings.logo_position.label",
      "info": "t:sections.header.settings.logo_position.info"
    },
    {
      "type": "link_list",
      "id": "menu",
      "default": "main-menu",
      "label": "t:sections.header.settings.menu.label"
    },
    {
      "type": "select",
      "id": "menu_type_desktop",
      "options": [
        {
          "value": "dropdown",
          "label": "t:sections.header.settings.menu_type_desktop.options__1.label"
        },
        {
          "value": "mega",
          "label": "t:sections.header.settings.menu_type_desktop.options__2.label"
        },
        {
          "value": "drawer",
          "label": "t:sections.header.settings.menu_type_desktop.options__3.label"
        }
      ],
      "default": "dropdown",
      "label": "t:sections.header.settings.menu_type_desktop.label",
      "info": "t:sections.header.settings.menu_type_desktop.info"
    },
    {
      "type": "select",
      "id": "sticky_header_type",
      "options": [
        {
          "value": "none",
          "label": "t:sections.header.settings.sticky_header_type.options__1.label"
        },
        {
          "value": "on-scroll-up",
          "label": "t:sections.header.settings.sticky_header_type.options__2.label"
        },
        {
          "value": "always",
          "label": "t:sections.header.settings.sticky_header_type.options__3.label"
        },
        {
          "value": "reduce-logo-size",
          "label": "t:sections.header.settings.sticky_header_type.options__4.label"
        }
      ],
      "default": "on-scroll-up",
      "label": "t:sections.header.settings.sticky_header_type.label"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.header__3.content"
    },
    {
      "type": "checkbox",
      "id": "enable_country_selector",
      "default": true,
      "label": "t:sections.header.settings.enable_country_selector.label"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.header__4.content"
    },
    {
      "type": "checkbox",
      "id": "enable_language_selector",
      "default": true,
      "label": "t:sections.header.settings.enable_language_selector.label"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.mobile_layout.content"
    },
    {
      "type": "select",
      "id": "mobile_logo_position",
      "options": [
        {
          "value": "center",
          "label": "t:sections.header.settings.mobile_logo_position.options__1.label"
        },
        {
          "value": "left",
          "label": "t:sections.header.settings.mobile_logo_position.options__2.label"
        }
      ],
      "default": "center",
      "label": "t:sections.header.settings.mobile_logo_position.label"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.header__5.content"
    },
    {
      "type": "checkbox",
      "id": "enable_customer_avatar",
      "default": true,
      "label": "t:sections.header.settings.enable_customer_avatar.label",
      "info": "t:sections.header.settings.enable_customer_avatar.info"
    }
  ]
}
{% endschema %}
